{
    "builds": [
        {
            "src": "/tg_bot/app.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/tg_bot/app.py"
        }
    ]
}